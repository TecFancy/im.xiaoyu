import React from 'react';

const Title = ({title}) => (
  <div className="page02-title">
    <i className="page02-title-icon_left"></i>
    <h3 className="page02-title-text">{title}</h3>
    <i className="page02-title-icon_right"></i>
  </div>
);

export default Title;
